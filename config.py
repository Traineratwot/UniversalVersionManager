import os

SEP = os.path.sep

PROGRAM_PATH = os.environ['APPDATA'] + SEP + 'UVM' + SEP
NODE_PATH = PROGRAM_PATH + 'node' + SEP
BIN_PATH = PROGRAM_PATH + 'bin' + SEP
TO_PATH_PATH = PROGRAM_PATH + 'toPath.exe'
